# Copy to .env (or export in your shell) — never commit the real values.

# Batch dashboard -> Settings -> General. One key per company, full account access.
BATCH_REST_API_KEY=

# Batch dashboard -> Settings -> General (per project).
BATCH_PROJECT_KEY=
